# Evaluación técnica Odoo 17

## Levantar el entorno

```bash
docker compose up -d
docker compose logs -f odoo   # esperar "HTTP service (werkzeug) running"
```

Abrir http://localhost:8069 y crear la base de datos (master password: `admin`).

_(Se completará con la documentación del módulo.)_
